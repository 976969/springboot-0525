<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qcby.springboot_0525.mapper.VerificationCodeMapper">

    <resultMap id="BaseResultMap" type="com.qcby.springboot_0525.entity.VerificationCode">
        <id column="id" property="id"/>
        <result column="target" property="target"/>
        <result column="code" property="code"/>
        <result column="type" property="type"/>
        <result column="used" property="used"/>
        <result column="expire_time" property="expireTime"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO verification_code (target, code, type, used, expire_time)
        VALUES (#{target}, #{code}, #{type}, #{used}, #{expireTime})
    </insert>

    <select id="selectLatestByTargetAndType" resultMap="BaseResultMap">
        SELECT * FROM verification_code
        WHERE target = #{target} AND type = #{type}
        ORDER BY create_time DESC
        LIMIT 1
    </select>

    <select id="selectValidCode" resultMap="BaseResultMap">
        SELECT * FROM verification_code
        WHERE target = #{target}
          AND code = #{code}
          AND type = #{type}
          AND used = 0
          AND expire_time &gt; NOW()
        ORDER BY create_time DESC
        LIMIT 1
    </select>

    <update id="updateUsed">
        UPDATE verification_code SET used = 1 WHERE id = #{id}
    </update>

</mapper>
